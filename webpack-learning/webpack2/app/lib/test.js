console.log('测试 require 的各种形式');
/*
// 这种形式的加载也是 OK 的。
require([], () => {
  var a = require('./lib/a');
  a.sayHello();

  // var b = require('./lib/b');
  // b.sayHello();
});

// 这种形式的加载也是 OK 的。
require(['./lib/b'], (b) => {
  b.sayHello();
});

// 这种形式的加载也是 OK 的。
// define(['./lib/b'], function (b) {
//   b.sayHello();
// });
*/
console.log('good, hello, xxx');
