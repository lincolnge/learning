language: node_js
node_js:
  - node
cache:
  apt: true
  directories:
  - node_modules
script:
  - npm install
after_script:
  - npm run test
after_success:
  - './node_modules/.bin/nyc report --reporter=text-lcov | ./node_modules/.bin/coveralls'