language: node_js
node_js:
  - node
cache:
  apt: true
  directories:
  - node_modules
script:
  - npm install
  - npm run test
after_success:
  # - './node_modules/.bin/nyc report --reporter=text-lcov | coveralls'
  - npm run coveralls